import { ThemeProvider } from "@fleetwood/components/providers/ThemeProvider";
import Footer from "@fleetwood/components/sections/Footer";
import Header from "@fleetwood/components/sections/Header";
import Hero from "@fleetwood/components/sections/Hero";
import { ChildContent } from "@fleetwood/types/ReactChildren";
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import MainNav from "@fleetwood/components/nav/MainNav";
import ParallaxBg from "@fleetwood/components/ParallaxBg";
import Timeline from "@fleetwood/components/sections/Timeline";
import Milestone from "@fleetwood/components/sections/timeline/Milestone";
import { twMerge } from "tailwind-merge";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({children}: Readonly<ChildContent>) {
  return (
    <html lang="en">
      <ThemeProvider>
        <body className={twMerge(
          inter.className,
          "flex flex-col items-center overflow-hidden"
          )}>
          <Header />
          <main className="h-fit w-full max-w-5xl px-12 pb-12 relative">
            {children}
          </main>
        </body>
      </ThemeProvider>
    </html>
  );
}
